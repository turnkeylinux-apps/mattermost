#include <turnkey/base>

nginx

postgresql
postgresql-contrib
postgresql-13-postgis-3

python3-bcrypt

adminer
php-fpm                 /* adminer run via php-fpm now */
php-cgi                 /* adminer depend */
php-pgsql               /* needed for adminer pg support */

webmin-postgresql       /* webmin is in base, give the user the option */
